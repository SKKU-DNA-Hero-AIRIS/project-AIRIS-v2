# project-AIRIS-v2

> **체형·시나리오 적응형 에어게이트: 입자 시뮬레이션과 블랙박스 최적화로 먼지 제거에 최적인 자세와 노즐 설정을 찾는다**

---

## 1. 프로젝트 개요

### 한 줄 요약

에어게이트(에어샤워)를 통과하는 사람의 **체형**과 **시나리오**(일반 성인, 임산부, 휠체어 사용자)에 따라, 몸에 붙은 먼지가 가장 잘 제거되는 **자세**와 **노즐의 높이·방향·세기**를 시뮬레이션 기반 최적화로 찾고, 그 결과를 학습해 실시간으로 안내하는 시스템을 만든다.

### 배경

현재의 에어게이트는 노즐 배치가 고정되어 있고, 사용자에게 "팔을 들고 천천히 돌아라" 정도의 일률적 안내만 한다. 그러나 다음과 같은 문제가 있다.

- 키가 다르면 같은 노즐이 닿는 부위가 달라진다.
- 임산부는 상체를 굽히기 어렵고, 휠체어 사용자는 서 있는 사람과 노출되는 부위 자체가 다르다.
- 팔을 내리면 겨드랑이와 옆구리가 가려지고, 노즐을 등지면 정면이 안 털린다. 자세에 따라 제거율 차이가 크다.

즉, **같은 게이트라도 사람에 따라 최적의 자세와 노즐 설정이 다르다.** 이 프로젝트는 그 차이를 정량적으로 찾아내고 실시간으로 적용하는 것을 목표로 한다.

### v1과의 차이

이전 프로젝트(project-AIRIS-MVP)는 생체·환경·시설 맥락으로 위험도를 추정해 게이트 세기를 조절하는 판단 문제였다. v2는 **물리적으로 먼지를 가장 잘 털어내는 설정을 찾는 최적화 문제**로 주제를 완전히 바꾼다. v1에서 재사용하는 것은 사람 검출·포즈 추정 파이프라인과 Streamlit 대시보드 틀뿐이다.

---

## 2. 연구 질문과 가설

### 주 질문

> 체형과 시나리오를 고려해 자세와 노즐 설정을 개인화하면, 고정 노즐 + 일률 안내 방식보다 먼지 제거율이 유의미하게 높아지는가?

### 하위 질문

| 번호 | 질문 | 검증 실험 |
|---|---|---|
| Q1 | 자세만 바꿔도 제거율이 얼마나 오르는가? 노즐만 바꾸면? 둘 다 바꾸면? | E4 |
| Q2 | 시나리오별 최적 자세는 서로 어떻게 다른가? | E4 |
| Q3 | 최적화 결과가 물리 상수 선택에 얼마나 민감한가? 순위가 안정적인가? | E3 |
| Q4 | 최적화 결과를 학습한 모델이 새로운 체형에 일반화되는가? | E5 |
| Q5 | 저비용 시뮬레이터의 결론이 고정밀 유체 시뮬레이션에서도 유지되는가? | E6 |

### 가설

- H1: 자세와 노즐을 동시에 최적화하면 기준선 대비 제거율이 20% 이상 오른다.
- H2: 큰 기하학적 효과(팔 들기, 몸 방향, 노즐 높이)는 물리 상수를 바꿔도 순위가 유지된다.
- H3: 회귀 모델이 예측한 설정은 개인별로 직접 최적화한 점수의 95% 이상을 달성한다.

---

## 3. 문제 정의

### 입력

| 항목 | 표현 | 출처 |
|---|---|---|
| 체형 | 키, 어깨 너비, 몸통 두께, 팔 길이, 다리 길이 (5개 실수) | 포즈 추정 또는 샘플링 |
| 시나리오 | `default`, `pregnant`, `wheelchair` | 사용자 선택 |

### 결정 변수

| 항목 | 표현 | 범위 |
|---|---|---|
| 자세 | 어깨 벌림·굽힘, 팔꿈치, 상체 기울기·회전, 고관절, 무릎 (7개 각도) | 시나리오별 상하한. 휠체어는 하체 고정 |
| 노즐 | 노즐 M개 × (높이, 좌우 각도, 상하 각도, 세기) | 벽면 위치는 고정, 높이 0.2~2.0m, 세기 0~1 |

### 목적 함수

```
score = Σ_부위 w_부위 · 제거율_부위  −  λ · 불편도
불편도 = Σ_관절 c_관절 · |각도 − 기본각도| / 범위
```

부위 가중치와 불편도 가중치는 시나리오별로 다르다. 임산부는 상체 굽힘에, 휠체어 사용자는 팔 높이에 큰 페널티를 둔다.

### 제약

- 시나리오별 관절 각도 범위 (`configs/scenarios.yaml`)
- 시나리오별 고정 자세 (휠체어: 고관절·무릎 90도)
- 노즐 세기 상한 (임산부: 복부 정면 노즐 60% 이하)

### 왜 강화학습이 아닌가

찾는 것이 **통과 중 한 번 정해지는 정적 자세**이므로 순차 의사결정 문제가 아니다. 상태 전이가 없는 "입력 → 점수" 함수의 최적화이고, 이 경우 블랙박스 최적화(CMA-ES)가 강화학습보다 단순하고 확실하다. 머신러닝은 최적화 결과를 새로운 체형에 **일반화**하는 회귀 단계에서 실제로 필요한 자리에 들어간다.

---

## 4. 전체 파이프라인

```
[오프라인: 최적화]
  체형·시나리오 샘플 수천 개
        ↓
  입자 시뮬레이터 (Taichi GPU, 후보 100개 배치 평가)
        ↓
  CMA-ES → 체형·시나리오별 최적 자세 + 노즐 설정
        ↓
  (체형, 시나리오, 최적 설정, 점수) 데이터셋

[학습]
  데이터셋 → 회귀 모델 (scikit-learn)
  입력: 체형 5개 + 시나리오 → 출력: 자세 7개 + 노즐 4M개

[실시간: 게이트 앞]
  카메라 → 포즈 추정 (YOLO11-pose) → 체형 추정 + 시나리오 선택
        ↓
  회귀 모델 → 자세 안내 (화면) + 노즐 명령 (시뮬레이션 또는 하드웨어)

[검증]
  패치 기준선 비교 · 물리 상수 민감도 · LBM 유체 솔버 재평가
```

---

## 5. 개발 항목

### 5.1 마네킹 모델 (`airis/sim/body.py`, 담당 B)

- 관절 17개를 기준으로 몸을 **캡슐(원통 + 반구)** 조합으로 표현한다. 몸통은 타원 캡슐, 팔다리는 원형 캡슐, 머리는 구.
- 체형 파라미터가 캡슐의 길이와 반지름을, 자세 파라미터가 관절 각도를 통해 캡슐의 위치와 방향을 결정한다 (순기구학).
- 캡슐 표면을 **패치** 수백~수천 개로 샘플링한다. 패치마다 위치, 바깥 법선, 면적, 소속 부위(머리·몸통 앞·몸통 뒤·팔·다리)를 갖는다.
- 휠체어 시나리오는 좌석 높이에 골반을 두고 하체를 앉은 자세로 고정한다. 휠체어 프레임은 가림용 캡슐로 추가한다.
- SMPL 같은 정밀 메시는 쓰지 않는다. 기류 모델 오차가 더 크기 때문에 얻는 것이 없다.

### 5.2 제트 유동 모델 (`airis/sim/jet.py`, 담당 B)

- **자유 제트**: 노즐 출구부터 포텐셜 코어(노즐 지름의 약 6배)까지는 출구 속도 유지, 이후 중심 속도는 거리에 반비례해 감쇠, 단면은 가우시안. 확산 반각 약 12도.
- **충돌 제트 보정**: 표면 근처에서는 자유 제트 대신 벽면 전단 분포로 치환한다. 제트가 정면으로 닿는 정체점은 전단력이 0에 가깝고, 실제로 강한 곳은 중심에서 제트 반지름 2~3배 떨어진 고리 영역이다. 이 보정이 없으면 "노즐을 정면으로 조준"하는 잘못된 최적해가 나온다.
- **펄스 제트**: 주기와 듀티로 on/off 변조. 특허 자료에서 연속 분사보다 제거 효과가 좋다고 보고됨. 옵션.
- 노즐 M개의 속도장을 합산한다. 난류 제트의 상호작용은 무시하며, 이 오차는 E6에서 확인한다.
- 모든 상수는 `configs/physics.yaml` 한 곳에 둔다.

### 5.3 입자 시뮬레이터 (`airis/sim/particles.py`, 담당 A)

프로젝트의 핵심. Taichi로 GPU 커널을 작성한다.

- **입자 초기화**: 후보 하나당 입자 2만 개를 패치 위에 면적 비례로 뿌린다. 입자마다 지름(로그 정규 분포, 중앙값 20µm), 임계 전단력(로그 정규 분포), 부착 상태, 소속 부위를 갖는다.
- **부착 → 이탈 판정**: 입자가 놓인 패치의 벽면 전단력이 그 입자의 임계값을 넘으면 이탈한다. 임계값 분포로 옷감의 거칠기와 섬유 사이에 박힌 입자를 근사한다 (rock'n'roll 모델의 단순화).
- **이탈 후 운동**: 속도장에서 항력(Schiller-Naumann)을 받아 적분. 캡슐과 충돌하면 재부착 확률로 다시 붙거나 튕긴다. 부스 밖으로 나가면 제거 확정.
- **배치 실행**: 후보 B개 × 입자 N개를 한 커널에서 처리한다. CMA-ES 한 세대(후보 100개)가 한 번의 GPU 호출이다. 후보 간 상태가 섞이지 않는지 테스트로 보장한다.
- **결정론**: 시드 고정. 같은 입력이면 같은 점수.
- **성능 목표**: 입자 2만 × 후보 100, 3초 시뮬레이션(1,500스텝)을 수십 ms~수백 ms 안에.
- **시각화 덤프**: 프레임별 입자 위치와 부착 상태를 npz로 저장 (최적화 중에는 끔).

### 5.4 패치 기준선 (`airis/sim/patch_baseline.py`, 담당 D)

- 입자 없이 패치별 유효 풍속만으로 제거율을 계산하는 numpy 평가기. 밀리초 단위.
- 유효 풍속 = 제트 속도 × 법선 각도 계수 × 가림 여부(캡슐 레이캐스팅).
- 역할: (1) 입자판 완성 전까지 최적화 파이프라인을 끝까지 돌리는 대체재, (2) 입자판 디버깅 시 비교 대상, (3) 입자판이 늦어질 때의 보험.

### 5.5 최적화 (`airis/optimize/`, 담당 C)

- **알고리즘**: CMA-ES (`cma` 패키지). 변수 30~40개, 미분 불가, 시뮬레이터가 빠르므로 적합.
- **인코딩**: 자세 변수(고정 자세 제외) + 노즐 M개 × 4를 이어붙여 [-1, 1]로 정규화.
- **세대 크기**: 100. 배치 평가 한 번이 한 세대.
- **종료**: 1만 회 평가 또는 50세대 정체.
- **로그**: 실험 ID, 커밋 해시, 설정 파일 해시, 세대별 최고점, 최종 설정을 `outputs/<exp_id>/`에 저장.
- **데이터셋 생성**: 체형 1,500개 × 시나리오 3개 = 4,500회 최적화. 체형은 인체 측정 통계 기반 정규분포에서 샘플링. 재개 가능하게 설계.

### 5.6 회귀 모델 (`airis/model/`, 담당 C)

- 입력: 체형 5개 + 시나리오 원핫 3개. 출력: 자세 7개 + 노즐 4M개.
- scikit-learn MLPRegressor와 HistGradientBoostingRegressor를 비교. 5-fold 교차검증.
- 평가는 출력값의 오차가 아니라 **예측한 설정을 시뮬레이터에 넣었을 때의 점수**로 한다 (E5).

### 5.7 실시간 파이프라인 (`airis/realtime/`, `airis/viz/`, 담당 E)

- v1의 웹캠 입력과 YOLO 사람 검출을 재사용하고, YOLO11-pose로 키포인트 17개를 추출한다.
- 키포인트에서 키, 어깨 너비, 팔 길이를 추정한다. 카메라 거리 보정은 기준 물체 또는 알려진 키 입력으로 한다.
- 시나리오는 화면에서 사용자가 선택한다 (임산부 여부를 영상으로 판별하지 않는다).
- 회귀 모델의 출력을 3D 마네킹으로 그려 안내하고, 노즐 설정은 대시보드에 표시한다.
- 개발용 3D 뷰(matplotlib)와 발표용 대시보드(Streamlit + plotly)를 나눈다.

### 5.8 LBM 검증 파이프라인 (`validation/lbm/`, 담당 D)

- Taichi-LBM3D를 확장해 부스 안 기류를 격자 볼츠만 방법으로 계산한다. 격자 128³, 마네킹은 침지 경계로 넣는다.
- 노즐은 출구 경계 조건으로, 부스 벽과 배기구도 경계로 넣는다.
- 1회 2~10초라 최적화 루프에는 못 쓴다. E6에서 상위 후보 재평가에만 쓴다.

---

## 6. 실험 계획

### E1. 정성 검증 (1주차, D)

시뮬레이터가 최소한 방향은 맞는지 확인하는 자동 테스트. 병합 게이트로 쓴다.

| 테스트 | 기대 결과 |
|---|---|
| 팔을 들면 | 겨드랑이·옆구리 제거율 상승 |
| 노즐을 등지면 | 정면 제거율 하락 |
| 노즐에 가까이 서면 | 제거율 상승 |
| 노즐 세기 0 | 제거율 0 |
| 같은 입력 두 번 | 같은 점수 |

### E2. 패치판 vs 입자판 일치도 (2주차, D)

- 무작위 설정 500개를 두 평가기로 평가하고 점수의 순위 상관(Spearman)을 본다.
- 0.8 이상이면 패치판을 대체재로 신뢰. 낮으면 어느 쪽이 틀렸는지 시각화로 추적.

### E3. 물리 상수 민감도 (3주차, C + D)

- 대상 상수: 임계 전단 중앙값, 임계 전단 분산, 제트 감쇠 상수, 재부착 확률, 입자 크기 중앙값.
- 각 상수를 기준값의 0.5, 0.75, 1, 1.5, 2배로 바꿔 시나리오 3개를 최적화.
- 지표: 상수 변화에 따른 **최적 자세의 변화량**과 **상위 10개 후보의 순위 안정성**.
- 결론: 순위가 안정적인 결론(예: 팔 들기)은 신뢰하고, 불안정한 결론은 "상수 의존"으로 표시한다. 실측이 필요한 상수 목록이 여기서 나온다.

### E4. 시나리오별 최적화와 기준선 비교 (3주차, C)

핵심 실험. 시나리오 3개 × 시드 5개.

| 조건 | 자세 | 노즐 | 의미 |
|---|---|---|---|
| B0 | 기본 자세 (팔 내림, 정면) | 고정 (현재 에어게이트) | 현재 기준선 |
| B1 | 업계 권장 (팔 들고 정면) | 고정 | 현재 최선의 안내 |
| C1 | 최적화 | 고정 | 자세 안내만의 효과 (Q1) |
| C2 | 기본 자세 | 최적화 | 노즐 조정만의 효과 (Q1) |
| C3 | 최적화 | 최적화 | 제안 방식 |

- 지표: 총 제거율, 부위별 제거율, 불편도, B0 대비 상대 개선율.
- 시나리오별 최적 자세를 3D로 그려 비교한다 (Q2).
- 시드 5개의 결과 분산으로 최적화 수렴 여부를 확인한다.

### E5. 일반화 (4~5주차, C)

- 데이터셋 4,500행으로 회귀 모델 학습. 홀드아웃 체형 300개.
- 홀드아웃 체형마다 (a) 회귀 예측 설정, (b) 직접 CMA-ES 최적화 설정을 시뮬레이터로 평가.
- 지표: **(a) 점수 / (b) 점수** 의 분포. 중앙값 0.95 이상이면 H3 채택.
- 추가: 포즈 추정에서 오는 체형 오차(±5%)를 넣었을 때 점수 하락폭.

### E6. LBM 검증 (5주차, D)

- 시나리오별로 E4의 C3 상위 10개 + B0 + B1을 LBM으로 재평가 (총 36케이스, 1~3시간).
- 지표: 입자판 점수와 LBM 점수의 순위 상관. B0·B1 대비 C3의 우위가 LBM에서도 유지되는가.
- 유지되면 결론의 신뢰도 확보. 안 되면 어느 물리 효과(몸 주변 회절, 노즐 간섭)가 원인인지 분석.

### 계산 예산

RTX 4090급 클라우드 3주 상시 (약 500 GPU시간, 25~40만 원). E3 20~40h, E4 10~20h, E5 데이터셋 100~250h, E6 30~60h, 여유 100h.

---

## 7. 일정과 역할

6주. 개발은 2주 안에 끝내고 3~4주를 계산과 실험에 쓴다.

| 역할 | 담당 영역 |
|---|---|
| A. 시뮬레이터 코어 | Taichi 입자 커널, 배치 실행 |
| B. 물리 모델 | 마네킹, 제트, 시나리오, 물리 상수 |
| C. 최적화·실험 운영 | CMA-ES, 클라우드, 민감도, 데이터셋, 회귀 |
| D. 검증·분석 | 패치 기준선, 테스트, LBM, 결과 분석, PR 리뷰 |
| E. 실시간·시각화·문서 | 포즈 추정, 3D 뷰, 대시보드, 발표 |

주차별 배치, 체크포인트, A가 밀릴 때의 대응은 [docs/plan.md](docs/plan.md)에 있다.

**체크포인트**
- 1주차 말: 패치판으로 최적화 스크립트가 끝까지 돈다. E1 통과.
- 2주차 말: 입자판 배치 평가가 같은 스크립트에 꽂힌다. 계산 시작.
- 3주차 말: E3 결과로 신뢰 가능한 결론 목록 확정. E4 완료.
- 4주차 말: 데이터셋 1차, 회귀 1차.
- 5주차 말: E5, E6 결과. 실시간 데모 동작.

---

## 8. 저장소 구조

```
airis/
  sim/
    types.py           # 공용 데이터 타입 (변경 시 전원 리뷰)
    interface.py       # Evaluator 추상 클래스
    body.py            # 마네킹 기하                     [B]
    jet.py             # 제트 속도장                     [B]
    scenario.py        # 시나리오·물리 상수 로더          [B]
    patch_baseline.py  # 패치 기반 빠른 평가기 (기준선)   [D]
    particles.py       # Taichi 입자 평가기               [A]
  optimize/
    cmaes_runner.py    # CMA-ES 루프                      [C]
    dataset.py         # 회귀용 데이터셋 생성              [C]
  model/               # 회귀 모델                        [C]
  viz/                 # 3D 시각화, 대시보드               [E]
  realtime/            # 포즈 추정 → 체형·시나리오          [E]
configs/
  physics.yaml         # 물리 상수 단일 소스              [B]
  scenarios.yaml       # 시나리오 제약                    [B]
validation/lbm/        # LBM 검증 파이프라인               [D]
tests/                 # 정성 검증 테스트 (병합 전 로컬 실행) [D]
scripts/               # 실행 스크립트                    [C]
docs/
  interfaces.md        # 타입·함수 계약, 데이터셋 스키마
  plan.md              # 주차별 계획, 역할, 체크포인트, 협업 규칙
```

---

## 9. 시작하기

```bash
python -m venv .venv
.venv/Scripts/activate      # Windows
pip install -r requirements.txt
pip install -e .
pytest -q
```

Taichi GPU 백엔드 확인:

```bash
python -c "import taichi as ti; ti.init(arch=ti.gpu); print(ti.lang.impl.current_cfg().arch)"
```

---

## 10. 협업 규칙

- `main`은 직접 push 불가. 기능 브랜치 → PR → D 리뷰 → 병합. 병합 전 `pytest -q` 통과.
- `airis/sim/types.py`와 `docs/interfaces.md`는 전원 리뷰 없이 바꾸지 않는다.
- `configs/physics.yaml`은 B 소유. 다른 사람은 PR로만 수정.
- 실험은 전부 기록: 실험 ID, 커밋 해시, 설정 해시, 결과 경로. C가 `docs/experiments.md`에 관리.
- 커밋 메시지: `feat|fix|test|docs|exp: 요약`.
- 주 2회 통합 점검. A와 B는 추가로 주 2회 페어.

---

## 11. 범위 밖과 한계

- **걷기·회전 동작**은 다루지 않는다. 정적 자세만 최적화한다.
- **옷의 펄럭임**(cloth 시뮬레이션)은 다루지 않는다. 부착력 분포로 근사한다.
- **노즐 구동 하드웨어**는 만들지 않는다. 노즐 설정은 시뮬레이션과 대시보드까지.
- **절대 제거율은 주장하지 않는다.** 시뮬레이터 상수가 실측으로 보정되지 않았으므로, 결과는 기준선 대비 상대 개선율로 제시한다.
- **미세한 각도 차이**(5도 수준)는 모델 오차에 묻힌다. 최적 자세는 점이 아니라 구간으로 제시한다.
- 시간이 되면 풍속계와 형광 분말 마네킹으로 물리 상수를 보정한다. 되지 않으면 E3의 민감도 결과로 대신한다.

---

## 12. 참고 문헌

**옷감에서의 입자 제거**
- Keedy et al. 2008, *Measurements of Air Jet Removal Efficiencies of Spherical Particles from Cloth and Planar Surfaces*, Aerosol Sci. Tech. [링크](https://www.tandfonline.com/doi/pdf/10.1080/02786820802402237)
- *Dust resuspension from fabrics exposed to airflow*, 2024, Aerosol Sci. Tech. [링크](https://www.tandfonline.com/doi/full/10.1080/02786826.2024.2324980)
- *Resuspension of inhalable particles from clothing: A manikin-based chamber study*, 2024, Building and Environment. [링크](https://www.sciencedirect.com/science/article/pii/S0360132324009995)

**충돌 제트와 벽면 전단**
- Young, Hargather, Settles 2013, *Shear stress and particle removal measurements of a round turbulent air jet impinging normally upon a planar wall*, J. Aerosol Sci. [링크](https://www.sciencedirect.com/science/article/abs/pii/S0021850213000979)
- *Wall shear stress measurements and parametric analysis of impinging wall jets*, 2012, Int. J. Heat Mass Transfer. [링크](https://www.sciencedirect.com/science/article/abs/pii/S0017931012004565)

**입자 부착·이탈**
- Reeks & Hall 2001, rock'n'roll resuspension model, J. Aerosol Sci.
- *Characterization of adhesion force in aerodynamic particle resuspension*, 2017, J. Aerosol Sci. [링크](https://www.sciencedirect.com/science/article/abs/pii/S0021850217304202)
- *A model for particle removal from surfaces with large-scale roughness in turbulent flows*, 2019, Aerosol Sci. Tech. [링크](https://www.tandfonline.com/doi/full/10.1080/02786826.2019.1692126)

**자유 제트**
- Pope, *Turbulent Flows*, Ch. 5.
- *A Generic Centerline Velocity Decay Curve for Initially Turbulent Axisymmetric Jets*. [링크](https://www.researchgate.net/publication/245356504_A_Generic_Centerline_Velocity_Decay_Curve_for_Initially_Turbulent_Axisymmetric_Jets)

**에어샤워 업계 자료**
- Clean Air Products, *Cleanroom Air Shower Design*. [링크](https://www.cleanairproducts.com/technical-papers/how-to-spec-an-air-shower)
- US Patent 7017226, *Dust removal apparatus* (펄스 제트 효과). [링크](https://image-ppubs.uspto.gov/dirsearch-public/print/downloadPdf/7017226)

**최적화·도구**
- Hansen 2016, *The CMA Evolution Strategy: A Tutorial*, arXiv:1604.00772. [링크](https://arxiv.org/abs/1604.00772)
- Taichi-LBM3D, Fluids 2022. [링크](https://doi.org/10.3390/fluids7080270)
